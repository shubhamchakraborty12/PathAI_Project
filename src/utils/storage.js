// Storage functions refactored to memory defaults for MongoDB Atlas operation
export function loadStateFromStorage(defaultState) {
  return defaultState;
}

export function saveStateToStorage(state) {
  // State persistence handled directly via MongoDB Atlas API endpoints
}
